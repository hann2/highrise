# Local environment variables. Copy to `.env` and fill in real values.
# `.env` is gitignored.

# ElevenLabs API key, used by the global `sound-effects`, `text-to-speech`, and
# `setup-api-key` skills. Obtain at https://elevenlabs.io/app/settings/api-keys
ELEVENLABS_API_KEY=
