# Config

This folder is for things that configure how stuff in `core/` works for this
game: rendering layers, custom event types, collision groups, and physics
materials. `core/` imports from here, so keep imports from game code type-only.
